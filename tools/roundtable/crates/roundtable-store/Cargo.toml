[package]
name = "roundtable-store"
version = "0.1.0"
edition = "2021"

[dependencies]
roundtable-protocol = { path = "../roundtable-protocol" }
rusqlite = { version = "0.40.1", features = ["bundled"] }
thiserror = "2"
